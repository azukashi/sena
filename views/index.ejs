<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <title>Sena Dash</title>

        <meta name="theme-color" content="#5865F2" />
        <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet" />

        <style>
            * {
                margin: 0;
                padding: 0;
                outline: none;
                user-select: none;
            }

            body {
                background-color: #000000;
                font-family: 'Roboto', sans-serif;
            }

            div.btn-submit {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                text-align: center;
            }

            label {
                background-color: #5865f2;
            }

            .swal2-popup .swal2-styled:focus {
                box-shadow: none !important;
            }
        </style>
    </head>

    <body>
        <div class="hero text-white text-center mt-32">
            <h1 class="text-4xl">Sena (セナ)*</h1>
            <p>Uplooad images with no hassle to server!</p>
        </div>
        <div class="btn-submit">
            <form id="submit" action="/" method="post" enctype="multipart/form-data">
                <input type="file" name="image" id="file" required hidden />
                <label class="mr-1 p-3 rounded-lg cursor-pointer text-white" for="file" id="fileLabel">➕</label>
            </form>
            <p class="text-white text-center mt-4">Supported extensions: *.png, *.jpg, *.jpeg, *.gif</p>
            <p class="text-white text-center mt-4">
                * App name is inspired from Blue Archive's character: Himuro Sena (氷室 セナ)
            </p>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script>
            document.getElementById('file').addEventListener('change', function (e) {
                e.preventDefault();
                document.getElementById('submit').submit();
                document.getElementById('submit').reset();
            });

            const onloadAlert = '<%= alert %>';

            if (onloadAlert.length > 0) {
                window.onload = function () {
                    Swal.fire({
                        title: onloadAlert,
                        width: 'auto',
                        showConfirmButton: true,
                        focusConfirm: false,
                        confirmButtonColor: '#5865F2',
                        confirmButtonText: `Okay!`,
                    });
                };
            }
        </script>
    </body>
</html>
